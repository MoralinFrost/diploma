package com.example.diploma.repos;

import com.example.diploma.Entity.CheckResult;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CheckResultRepos extends JpaRepository<CheckResult, Integer> {
}
